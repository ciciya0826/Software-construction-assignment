### 1. 软件（Software）

**核心定义**：计算机系统中依托硬件运行的逻辑集合，由程序、数据和文档共同构成，用于实现特定功能或解决特定问题。

软件=程序+数据+文档

软件按照功能划分为系统软件，支撑软件和应用软件。
软件按照工作方式划分为实施软件，分时软件，交互式软件和批处理软件。

程序设计活动包括分析问题，设计算法，编写程序，测试与排错



### 一、模块化原则核心内容

模块可分解性、模块可组装性、模块可理解性、模块连续性、模块保护性

将软件系统拆解为**独立、可复用、功能单一**的模块，每个模块聚焦完成一个核心任务；模块间通过清晰、标准化的接口交互，核心目标是降低系统复杂度，提升可维护性、可复用性和可扩展性。

### 二、内聚性

#### 概念

模块内部各元素（语句、功能、数据）之间结合的紧密程度，内聚度越高，模块功能越单一、独立性越强，是衡量模块 “内部质量” 的核心指标。

#### 内聚类型（从低到高，最优为功能内聚）

| 类型     | 核心特征                                                     |
| -------- | ------------------------------------------------------------ |
| 偶然内聚 | 模块元素无逻辑关联，仅因代码凑在一起组合（最差）             |
| 逻辑内聚 | 模块包含若干逻辑相关功能，通过参数选择执行某一个，单入口多功能函数 |
| 时间内聚 | 模块元素因需在同一时间段执行（如初始化、清理）而组合         |
| 过程内聚 | 模块按特定流程步骤执行多个相关功能                           |
| 通信内聚 | 模块元素操作同一数据集合（如同一文件 / 变量）                |
| 顺序内聚 | 模块一个功能的输出是另一个功能的输入，按顺序执行             |
| 功能内聚 | 模块仅完成一个独立、完整的核心功能（最优）                   |

### 三、耦合性

#### 概念

函数之间的调用关系是反映模块耦合性的最重要因素之一。模块之间相互依赖、相互影响的程度，耦合度越低，模块独立性越强，系统越易维护，是衡量模块 “外部关联程度” 的核心指标。会强烈的影响系统的可理解性、可测试性、可靠性和可维护性。

#### 耦合类型（从高到低，最优为数据耦合 / 无耦合）

| 类型       | 核心特征                                                     |
| ---------- | ------------------------------------------------------------ |
| 内容耦合   | 一个模块直接访问 / 修改另一个模块的内部数据 / 代码（最差）   |
| 公共耦合   | 多个模块共享公共数据环境（全局数据结构、共享通信区等）       |
| 外部耦合   | 多个模块都访问同一全局简单变量，且不是通过参数传送该全局变量信息 |
| 控制耦合   | 一个模块通过参数传递控制信息，明显的控制和选择另一函数的功能 |
| 标记耦合   | 模块间传递一个数据结构（或其子结构），而非单个基础数据       |
| 数据耦合   | 模块间仅传递基本数据（如数值、字符串），无额外依赖（较优）   |
| 非直接耦合 | 模块间无任何依赖，只是受控于一个主程序（最优）               |



黑盒测试是一种**不关注程序内部实现细节**的软件测试方法，测试人员以 “用户视角”，仅依据需求规格说明书，通过预设输入数据、观察输出结果，验证程序是否符合预期的功能、性能、兼容性等需求。

### 1. 基于等价类划分的测试

**概念**：黑盒测试中最基础的用例设计方法，核心是将程序的**输入域划分为若干个等价类**，从每个等价类中选取少量代表性数据作为测试用例，用少量用例覆盖同一类的所有输入场景。

- **核心思想**：同一等价类内的所有输入数据，对程序的处理逻辑和输出结果是 “等价” 的，测试一个用例即可代表整个类的测试效果。
  - 有效等价类：符合需求规格的输入数据，用于验证程序的正常功能；
  - 无效等价类：不符合需求规格的输入数据，用于验证程序的异常处理能力。

### 2. 基于边界值分析的测试

**概念**：针对程序输入 / 输出的**边界值**设计测试用例的方法，核心是聚焦输入域的边界（如数值范围的极值、长度的临界值等），优先测试边界附近及边界上的数据。

- **核心思想**：程序的错误往往集中在输入域的边界处（如`年龄范围18-60岁`，错误常出现在 17、18、60、61 这些值），通过测试边界值可高效发现潜在问题。
- **核心测试对象**：包括输入域的最大值、最小值、刚好超出边界的值、边界内临近值等。



### 一、抽象（Abstraction）

抽象指**忽略事物的非本质细节，仅提取与目标需求相关的核心特征，形成对事物的简化描述**。它分为两种核心类型：**过程抽象**：隐藏操作的具体实现步骤，仅暴露操作的功能.**数据抽象**：隐藏数据的存储结构和细节，仅暴露操作数据的接口（值及其运算）

#### 目的

1. **降低复杂度**：
2. **聚焦核心需求**：明确事物的核心功能，简化系统的设计与理解
3. **提升复用性**

### 二、封装（Encapsulation）

#### 概念

封装是将**数据（属性）和操作数据的方法（行为）绑定在一起**， 仅通过预设的公开接口对外提供访问。

#### 目的

1. **保护数据安全**：避免外部直接修改对象内部数据，防止非法操作导致的数据错误
2. **降低模块耦合**：模块内部实现修改时，只要保持公开接口不变，就不会影响外部调用，提升系统的可维护性；
3. **简化外部使用**：调用公开接口即可完成操作



### Junit基本功能，原理

是java语言的测试框架，实质上就是包含测试用例，测试执行，测试记录的Java类和接口，程序员只需要编码继承其中的类或者实现其中接口，抽象类就可以通过注解 + 断言实现测试、自动运行并输出结果。

### 参数化测试作用，用法

Junit同时支持参数化测试，参数化测试能够通过较少的代码完成测试覆盖，提高代码复用率，在junit中使用需要
@ParameterizedTest`替代普通测试的`@Test，并且使用valuesource指定单一简单参数或者csvsource指定多重参数



### 面向对象的设计原则

| 原则         | 内容                                                         |
| :----------- | :----------------------------------------------------------- |
| 单一职责原则 | 一个类只有一种单一功能，不要实现过多的功能                   |
| 开放封闭原则 | 软件实体（模块、函数、类）应该可以扩展（继承、合成），但不可修改 |
| 依赖倒转原则 | 抽象不应该依赖细节，细节应该依赖抽象                         |
| 里氏代换原则 | 子类型必须能够替换它们的基类型，保证继承复用的基础，违反则必然导致违反开放封闭原则 |
| 接口隔离原则 | 不应强迫客户依赖于它们不用的方法，接口属于客户，不属于它所在的类结构层次 |

#### 1.开放封闭原则：

是松散耦合的具体体现，核心是共性和个性分析，关键是抽象，里氏代换原则是开放封闭原则的具体实现手段之一

#### 2.合成复用原则：

继承会提高耦合度，破坏信息隐藏的封装性，因此尽量使用合成/聚合关系，而不是使用继承



### 防御性编程和断言

断言是让程序在运行过程中进行自我检查的代码，主要通过验证内部假设（如参数合法性，状态正确性）提前规避逻辑错误，避免逻辑扩散

| 断言类型 | 内容                       | 作用                                 |
| -------- | -------------------------- | ------------------------------------ |
| 前置断言 | 代码执行之前必须具备的特性 | 主要用于检查方法前置条件，如参数范围 |
| 后置断言 | 代码执行之后必须具备的特性 | 检查方法执行后的后置结果             |
| 不变断言 | 代码执行前后不能变化的特性 | 确保对象状态不变量（属性合法）       |

防御性编程的原理：保护程序不被无效输入破坏，主要检查每个输入参数的数据，一旦发现无效数据就直接进行错误处理或者使用异常

### 表驱动编程

表驱动编程是指把使用逻辑语句的复杂条件存放到表中，而在代码中仅通过选择条件得到复杂逻辑结果，表项的内容可以是直接结果，也可以是动作，而表项的查询方式包括直接访问，数组下标直接访问
还有就是阶梯式访问，表中的每个项代表的是一个数据范围而不是单个数据，如90-100对应优秀，可以通过适当的逻辑形式进行一一对应（如整数除法）。
最后就是索引访问，如哈希表或者散列表等。

### 基于程序结构的测试

| 测试方法     | 准则                                            | 目标                         | 覆盖度             | 优势                   |
| ------------ | ----------------------------------------------- | ---------------------------- | ------------------ | ---------------------- |
| 语句覆盖测试 | 所有可执行语句至少执行一次                      | 确保无未执行的死代码         | 低（仅覆盖语句）   | 简单易实现             |
| 判定覆盖测试 | 每个判定的 “真 / 假” 分支至少各执行一次         | 验证所有判定的分支逻辑       | 中（覆盖判定分支） | 覆盖度优于语句覆盖     |
| 条件覆盖测试 | 每个简单布尔条件的 “真 / 假” 取值至少各出现一次 | 验证条件取值对判定结果的影响 | 中（覆盖条件取值） | 关注条件细节           |
| 路径覆盖测试 | 所有独立执行路径至少执行一次                    | 覆盖程序全部执行逻辑         | 高（覆盖所有路径） | 覆盖最全面、验证最充分 |

语句覆盖测试是遍历所有节点
判定覆盖测试是对于所有分支均经历一遍
条件覆盖则是是针对每个基判定条件的正确与错误均覆盖一遍 比如说if（a&&b），那么a和b都是基判定条件
路径覆盖测试是从起点到终点的所有可能路径全部遍历一遍

##### 参数化测试：

只写一个测试方法，把测试用例作为参数传递进去，循环测试多个测试数据



### 交互设计的原则

#### 三基本：

**1.可学习性：**新用户能够用其进行有效交互并且发挥最大性能
**2.灵活性：**用户和系统能够用多种方式交换信息
**3.健壮性：**面对用户的错误操作，依旧能够保持交互流程的一致性和连贯性。

#### 八黄金：

**1.尽量保持一致**。界面，术语，字体，布局等在不同界面的表现尽量一致，容易理解并且数量有限
**2.满足普遍可用性**。设计的界面尽量考虑用户的年龄范围、身体状况、操作的熟练程度等因素
**3.提供信息反馈**。对用户每个动作都要有清晰的反馈
**4.设计对话框显示结束信息**。设计能够终止的交互会话，使用户知道什么时候完成任务
**5.预防并且处理错误**。尽可能让用户不犯错，用户犯错应该尽量提供指导回复
**6.允许撤销操作**。尽量让操作容易撤回
**7.支持内部控制点**。用户操作尽量有反馈或结果，熟悉的操作尽量不要更改
**8.减轻短时记忆负担**。界面显示尽量简单，不同界面的显示风格应该一致

### 快速原型法：

1.原型是目标系统的初步形态，形式或实例，是最终目标系统的模型展示。
2.快速原型法：快速原型法是通过快速建立可以在计算机上运行的程序，使用户能够直观看到软件交互的形式和使用效果，进而明确、补充和精细化交互需求来支持开发过程，开发者通过反复迭代得到最终用户满意的模型（不一定是最终系统）
**3.快速原型法的四个步骤:**
1.功能选择（根据用户对交互的基本需求，给出初步的交互设计）
定义好去实现的功能后，其他相关功能可以模拟实现，不相关的功能不去实现
2.构造原型
设计并且实现满足用户提出基本要求的软件系统，并且重点关注预期的设计评估，不考虑长期使用。
3.运行和评价原型
4.修改和完善原型（消除用户需求的不确定性）

### 基本的静态测试：

| 类别     | 核心定义                                                     |
| -------- | ------------------------------------------------------------ |
| 桌面检查 | 开发者**个人独立完成**的自我审查，在编写完代码 / 文档后，自行对照规范、需求检查语法错误、逻辑漏洞、格式问题，无他人参与。 |
| 代码走查 | 由**项目团队成员共同参与**的协作审查：开发者讲解代码逻辑，团队成员通过提问、讨论的方式共同排查问题，主要通过人脑模拟而不是测试用例 |
| 正式审查 | 遵循**严格标准化流程**的严谨审查：设置明确角色（主持人、审查员、记录员、作者），需经过会前准备、会议审查、缺陷跟踪、报告输出等环节，审查结果形成正式文档，是最规范的静态测试方式。 |
| 同行评审 | 由**同级别开发人员一对一 / 小范围**开展的针对性审查：比如结对编程中的互查、同事间的代码互审，流程灵活，侧重针对性发现特定模块的问题，介于桌面检查和代码走查之间。 |





### 一、集成策略核心定义

集成策略是软件集成测试中，**模块间集成的顺序、方式与实施逻辑**的总称，核心目标是通过合理规划集成顺序，高效验证模块接口兼容性、协同工作逻辑，尽早发现集成缺陷（如接口参数不匹配、依赖冲突）。

### 二、主要集成策略的核心定义与实施步骤

#### 1. 自顶向下集成策略

- **核心定义**：从软件架构的顶层控制模块（如主程序、核心流程模块）开始，逐层向下集成下层模块，未集成的下层模块用**桩模块**（模拟接口的临时模块）替代。
- 实施步骤
  1. 集成顶层核心模块，用桩模块替代所有下层依赖模块；
  2. 按依赖关系，逐步用真实模块替换桩模块，每替换一次执行集成测试；
  3. 重复直至所有模块集成完成。

#### 2. 自底向上集成策略

- **核心定义**：从软件架构的底层基础模块（如工具类、数据操作模块）开始，逐层向上集成上层模块，未集成的上层模块用**驱动模块**（模拟调用逻辑的临时模块）替代。
- 实施步骤
  1. 集成底层独立模块，直接测试其功能正确性；
  2. 用驱动模块模拟上层调用，将已集成模块组合成子系统并测试；
  3. 逐步替换驱动模块为真实上层模块，直至全系统集成。

#### 3. 基干集成策略

- **核心定义**：结合自顶向下与自底向上的混合策略，以中间核心业务模块为分界，上层自顶向下集成，下层自底向上集成，最终在中间层汇合。
- 实施步骤
  1. 确定中间核心业务模块，作为集成汇合点；
  2. 下层基础模块自底向上集成至中间层，上层控制模块自顶向下集成至中间层；
  3. 中间层与上下层对接，完成全系统集成测试。

#### 4. 一次性集成策略

- **核心定义**：待所有模块开发、单元测试完成后，一次性将所有模块整合为完整系统，再执行集成测试。出现错误难以发现也难以修改，要检查所有模块
- 实施步骤
  1. 等待所有模块开发与单元测试完毕；
  2. 一次性整合所有模块，部署为完整系统；
  3. 执行整体集成测试，排查接口与协同问题。

#### 5.增式集成

- **核心定义**：优先集成软件的核心业务模块（如电商下单 - 支付流程），验证核心功能稳定后，再逐步集成周边辅助模块。
- 实施步骤
  1. 识别并集成核心业务模块，搭建最小可行系统；
  2. 对核心系统进行全面测试，确保核心流程无缺陷；
  3. 按优先级依次集成周边模块，每集成一个执行回归测试。

### 三、各集成策略优缺点对比表

| 策略名称     | 优点                                   | 缺点                                 |
| ------------ | -------------------------------------- | ------------------------------------ |
| 自顶向下集成 | 集成的任何时候都有可以运行的程序       | 桩模块开发工作量大；底层问题发现晚   |
| 自底向上集成 | 无需桩模块；底层问题早发现；测试效率高 | 驱动模块开发工作量大；整体流程验证晚 |
| 基干集成     | 兼顾流程验证与底层可靠性；适用范围广   | 集成流程复杂；团队协调成本高         |
| 一次性集成   | 流程简单；无需开发辅助模块             | 问题定位难度大；风险集中；容错率低   |
| 增式集成     | 核心功能早验证；降低项目整体风险       | 核心模块定义需清晰；周边集成易冲突   |





# 持续集成（CI）核心内容

## 一、持续集成对软件开发的价值

1. 降低开发风险：更早发现并修正缺陷，减少不合理假设。
2. 减少重复过程：节省时间、资金及人力成本。
3. 保障交付能力：任何时间可生成可运行、可交付的软件。
4. 提升项目可见性：提供实时构建状态与系统质量，辅助有效决策。
5. 建立产品信心：在开发过程中强化对产品质量的把控。

## 二、实施持续集成的 4 个步骤

1. 识别：明确需自动化的活动（如编译、测试、审查、部署、数据库集成等）。
2. 构建：编写构建脚本，确保自动化过程重复且一致。
3. 分享：通过版本控制系统，实现脚本的团队共享。
4. 持续：代码变更后（自动 / 手工）运行构建脚本，提交至版本控制中心。

## 三、实施持续集成的 7 项最佳实践

1. 频繁提交代码：遵循 “早集成、常集成”，优先小任务开发，完成即提交，减少集成耗时。
2. 不提交有缺陷代码：提交前在本地运行个人构建，确保代码可正常运行。
3. 立即修复故障构建：构建失败（编译错误、测试失败等）需第一时间修复。
4. 编写自动化开发者测试：依托 JUnit 等框架，实现测试全自动化。
5. 确保所有测试与审查通过：含编码规范在内的审查、测试需全部达标。
6. 提交前执行个人构建：提前拦截缺陷，避免故障构建流入版本库。
7. 规避缺陷代码传播：禁止提交构建失败的代码，防止影响团队协作。





### 代码重构

是指在**不改变代码外部功能和行为**的前提下，优化代码内部结构、逻辑组织、命名规范等细节的过程。

### 核心目标

是提升代码的可读性、可维护性、可扩展性，降低技术债务和后续开发 / 维护成本，而非新增功能或修复 bug。

### 代码重构的核心重要性

1. **降低维护成本**：优化代码结构、命名与逻辑，提升可读性，减少后续理解、修改代码的时间成本。
2. **提升可扩展性**：解耦混乱模块、梳理冗余逻辑，新增功能时无需 “牵一发而动全身”，降低耦合度，提高内聚性。
3. **化解技术债务**：及时修复 “代码异味”（重复代码、过长函数等），避免小问题累积成难以维护的隐患。
4. **降低故障风险**：清晰的代码结构减少逻辑漏洞，便于测试和排查 bug，提升系统稳定性。
5. **提升协作效率**：统一代码规范，降低团队成员间理解、交接代码的成本。

| 测试层次 | 核心概念                                                     |
| -------- | ------------------------------------------------------------ |
| 单元测试 | **测试对象**：软件中最小的可测试单元（如函数、方法、类）；**核心目标**：验证单个单元的逻辑正确性，确保其独立功能符合设计；**关键特征**：由开发者自测，通常使用桩 / 驱动模块隔离外部依赖，聚焦单元内部逻辑。 |
| 集成测试 | **测试对象**：多个已测试单元组成的模块或模块间的接口；**核心目标**：验证模块间的接口兼容性、数据传递正确性，以及协同工作的逻辑是否符合预期；**关键特征**：关注模块间的交互，发现集成过程中的接口缺陷（如参数不匹配、依赖冲突）。 |
| 系统测试 | **测试对象**：完整的、集成后的软件系统；**核心目标**：验证系统整体是否符合需求规格说明书，覆盖功能、性能、兼容性、安全性等全维度质量指标；**关键特征**：模拟真实运行环境，从用户视角验证系统整体能力。 |
| 验收测试 | **测试对象**：最终可交付的完整系统；**核心目标**：由用户 / 客户验证系统是否满足业务需求，是否具备交付使用的条件；**关键特征**：聚焦业务场景而非技术细节，通常分为 Alpha 测试（内部验收）和 Beta 测试（用户现场验收）。 |

### 测试驱动开发（TDD原理）

原理：在开发功能代码前先编写单元测试代码，（只编写使测试通过的功能代码）确定需要编写什么应用代码。

特点：1.只开发满足需求的软件，要求厘清含糊的，抽象的需求，使其具体可测试
            2.促使实现低耦合设计，软件容易测试，并且测试相对独立
            3.尽早的避免，发现和修改错误
			4.与软件同步的文档
			5.TDD使开发者更易看见成果，减轻开发者压力，提高软件开发效率。



### GUI基本设计规范

1.界面合理:布局和颜色搭配合理，组件整齐有序
2.风格一致：使用标准的组件，明确定义的术语，与用户的习惯认知和软件的应用领域一致
3.元素标准：常见有窗口与对话框的标准、菜单和工具条的标准、图标的标准、基本组件的标准

### MVC模式

### 一、MVC 简述

MVC（模型 - 视图 - 控制器）是一种以「分离关注点」为核心的软件设计模式，将应用程序拆分为模型（Model）、视图（View）、控制器（Controller）三个独立模块，降低模块耦合度，提升应用的可维护性与扩展性。

### 二、各部分核心作用

#### 1. Model（模型）

核心作用：处理应用的核心数据（包括数据的存储、获取、校验），并封装核心业务逻辑，是应用的核心数据与业务处理中心。

#### 2. View（视图）

核心作用：负责将模型提供的数据渲染为用户可交互的界面，接收用户的操作行为并转发，是应用的界面展示与用户操作接收层。

#### 3. Controller（控制器）

核心作用：作为视图与模型之间的协调桥梁，接收视图转发的用户操作请求，调用模型处理业务逻辑，接收模型返回的处理结果并传递给视图，驱动视图刷新展示，是应用的交互调度层。

### 三、各部分的交互关系

遵循单向数据流逻辑：用户操作视图 → 视图将请求转发给控制器 → 控制器调用模型处理业务 → 模型返回处理结果给控制器 → 控制器将数据传递给视图 → 视图渲染数据展示给用户。核心依赖关系：视图依赖控制器，控制器依赖模型和视图，模型无依赖于视图和控制器。

### 四、MVC 的核心优势

1. 关注点分离：各模块职责单一清晰，降低耦合，便于分工协作。
2. 高复用性：模型层的业务逻辑可被多个视图复用，视图组件也可适配不同模型数据。
3. 易维护调试：模块解耦，问题可精准定位到对应层，修改成本低。
4. 可测试性强：各层独立，无需依赖完整应用环境即可开展测试。
5. 扩展性好：新增或修改功能仅需调整对应层，技术栈替换成本低。

### 事件驱动编程

在swing里面使用事件类（EVENT）表示某种事件，使用侦听器类（Listener）来捕捉和处理事件
用户对于组件的动作或者组件状态的改变都称为事件
发生动作或者状态改变的这个事件就是事件源




### 设计模式中的适配器模式

### 一、适配器模式核心定义

适配器模式（Adapter Pattern）是结构型设计模式的一种，核心作用是**解决两个不兼容接口之间的协作问题**—— 通过引入一个「适配器」类，将一个类的接口转换成客户端期望的另一个接口，让原本因接口不匹配而无法协同工作的类能够正常交互，类比现实中的电源适配器（将 220V 交流电转换为设备所需的 5V 直流电）。

### 二、核心角色

1. **目标接口（Target）**：客户端期望使用的统一接口，是适配器最终要适配成的形态；
2. **适配者（Adaptee）**：现有但接口不符合客户端要求的类 / 对象，是需要被适配的「原始组件」；
3. **适配器（Adapter）**：核心角色，实现目标接口，并持有适配者的引用，负责将客户端对目标接口的调用转换为对适配者接口的调用，完成接口转换和逻辑适配。

### 三、两种实现方式

#### 1. 类适配器（继承实现）

适配器同时继承适配者类、实现目标接口，通过继承复用适配者的方法，通过实现目标接口满足客户端调用规范。（注：因 Java 等语言不支持多继承，类适配器仅适用于适配者为类且单继承场景）

#### 2. 对象适配器（组合实现）

适配器实现目标接口，内部持有适配者对象的实例，通过组合的方式调用适配者的方法完成适配，是更灵活、常用的实现方式（符合「合成复用原则」）。

### 四、核心执行逻辑

1. 客户端按照目标接口的规范调用适配器的方法；
2. 适配器接收调用后，将其转换为适配者能识别的接口调用；
3. 适配者执行具体逻辑并返回结果；
4. 适配器将适配者的返回结果转换为目标接口要求的格式，返回给客户端。

### 五、核心优势

1. **接口兼容**：无需修改现有适配者代码，即可让其与目标接口协同工作，符合「开闭原则」；
2. **解耦客户端与适配者**：客户端仅依赖目标接口，无需关注适配者的具体实现，降低耦合；
3. **复用现有代码**：充分利用已有的适配者类，避免重复开发相同功能；
4. **灵活性高**：对象适配器可适配多个不同的适配者（或其子类），扩展成本低。

### 六、典型适用场景

- 集成第三方库 / 组件：第三方组件接口与项目现有接口不一致，通过适配器适配；
- 系统升级兼容：旧模块接口无法直接替换，用适配器适配新接口，保证旧代码可用；
- 多类接口统一：多个功能相似但接口不同的类，通过适配器统一为目标接口，降低客户端调用复杂度（如不同支付渠道（微信、支付宝）接口不同，适配为统一的「支付接口」）。

### 框架和设计模式

​     设计模式是对在某种环境下反复出现的问题以及解决方案的描述，不能运行，比框架更加抽象，框架通常使用代码表示，可以全部或者部分执行。设计模式是比框架颗粒更小的元素，一个框架往往含有多种设计模式，框架是针对某一特殊的应用领域，同一个设计模式可以适用于多种应用。可以说，框架是软件，设计模式是软件的知识。